[project]
name = "enae-vet-es"
version = "0.1.0"
description = "ENAE VET — chatbot Flask + LangChain"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "flask>=3.0.0,<4.0.0",
  "langchain-core>=0.3.0,<0.4.0",
  "langchain-anthropic>=0.3.0,<0.4.0",
]
